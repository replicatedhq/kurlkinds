installer:
  spec:
    kubernetes:
      version: latest
    containerd:
      version: latest
    weave:
      version: xxx
output:
  - message: Unknown weave add-on version xxx
    field: spec.weave.version
    type: unknown-addon
    patch:
      - op: replace
        path: /spec/weave/version
        value: 2.8.1
